{
  "_experience": {
    "decisioning": {
      "propositionEventType": {
        "interact": 1
      },
      "propositionAction": {
        "id": "{{offerId}}",
        "tokens": ["{{trackingToken}}"]
      },
      "propositions": [
        {
          "id": "{{subPropositionsId}}",
          "scope": "{{scope}}",
          "scopeDetails": {
            "characteristics": {
              "eventToken": "{{eventToken}}"
            },
            "activity": {
              "id": "{{activity_id}}",
              "matchedSurfaces": ["{{matchedSurfaces}}"]
            },
            "correlationID": "{{correlationId}}",
            "decisionProvider": "AJO"
          }
        }
      ]
    }
  }
}
